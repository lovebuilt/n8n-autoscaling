# Secrets — NEVER commit
.env
.env.*
!.env.example

# Claude Code local state
.claude/

# Backup artifacts (prevent accidental commits)
*.log
*.dump
*.tar.gz
/backups/

# OS artifacts
.DS_Store
*.swp
*.swo
*~


# Generated build files (created by custom/build.py, not edited manually)
Dockerfile.build
Dockerfile.runner.build
n8n-task-runners.build.json
.build-backups/
